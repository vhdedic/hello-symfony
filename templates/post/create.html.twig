{% extends 'base.html.twig' %}

{% block title %}Create post{% endblock %}

{% block body %}
    
    <h1 class="custom-page-title">Create New Post</h1>
    {{ form_start(form, { 'attr': {'class': 'custom-form custom-border'} }) }}
        {{ form_rest(form) }}

        <button type="submit" class="custom-button" formnovalidate>Save</button>
        <a href="{{ path('app_post_index') }}">
            <button type="button" class="custom-button">Cancel</button>
        </a>
    {{ form_end(form) }}
    
{% endblock %}
